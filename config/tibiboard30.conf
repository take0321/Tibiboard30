# --- Ver 3.2 の設定 ---

# Deep Sleep を有効にする
CONFIG_ZMK_SLEEP=y
# 30分 (1,800,000ms) でDeep Sleepへ (テストなら1分 60000 でもOK)
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=60000

# Idle (浅い眠り) は30秒
CONFIG_ZMK_IDLE_TIMEOUT=30000

# Bluetooth接続を強化
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# ZMK Studio (使うなら)
CONFIG_ZMK_STUDIO=y
