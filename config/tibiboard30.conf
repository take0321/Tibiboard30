# --- デバッグ・ログ出力（必須） ---
# これを入れておけば、動かない時に理由がわかる
CONFIG_ZMK_USB_LOGGING=y
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# --- 電源管理（トラブルの元なので一旦切る） ---
CONFIG_ZMK_SLEEP=n

# --- 機能 ---
# 念のためStudioは残しておく（キー確認に便利だから）
CONFIG_ZMK_STUDIO=y

# バッテリーやBLE設定も一旦デフォルトに戻す（記述しない）
